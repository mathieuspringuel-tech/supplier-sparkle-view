

## Remove Download Spec Button

Remove the "Download Spec" button and its handler from `src/pages/Index.tsx`.

### Changes
**`src/pages/Index.tsx`**: Delete the `handleDownloadSpec` function and the button element in the header. Keep the "Refresh Data" button as-is.

